# Assertions for 0015-rebrand-attribution.patch
#
# After `make build`, the user-facing startup/dashboard attribution must read
# as Nadia/NadicodeAI product ownership. Upstream provenance can stay in README
# fork notices, but it must not appear in live CLI or desktop product chrome.
# Paths are post-rename (hermes_cli/ -> nadia_cli/; ui-tui/ and web/ keep their
# names).

# --- rich CLI banner (nadia_cli/banner.py) ---
# Plain text, NOT an OSC-8 [link=...]: the banner's no-release-tag path asserts
# it emits no terminal hyperlink (tests/nadia_cli/test_banner.py). The clickable
# link lives on the HTML surfaces (footer + login) instead.
path:nadia_cli/banner.py nadicode.ai
path:nadia_cli/banner.py Nadia Agent by NadicodeAI (nadicode.ai)

# --- Ink TUI banner tagline (ui-tui/src/components/branding.tsx) ---
path:ui-tui/src/components/branding.tsx Nadia Agent by NadicodeAI
path:ui-tui/src/components/branding.tsx · NadicodeAI

# --- web dashboard footer (link + org label across locales) ---
path:web/src/components/SidebarFooter.tsx https://nadicode.ai
path:web/src/components/SidebarFooter.tsx Nadia Agent by NadicodeAI
path:web/src/i18n/en.ts org: "NadicodeAI"
path:web/src/i18n/zh.ts org: "NadicodeAI"

# --- dashboard login page wordmark + product line ---
path:nadia_cli/dashboard_auth/login_page.py nadicode.ai
path:nadia_cli/dashboard_auth/login_page.py Nadia Agent by
