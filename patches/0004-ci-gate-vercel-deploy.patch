Index: argo/.github/workflows/deploy-site.yml
===================================================================
--- argo.orig/.github/workflows/deploy-site.yml
+++ argo/.github/workflows/deploy-site.yml
@@ -22,14 +22,26 @@ concurrency:
 
 jobs:
   deploy-vercel:
-    if: github.event_name == 'release'
+    # Nadicode fork: gated off — upstream domain hermes-agent.nousresearch.com
+    # is owned by NousResearch. The VERCEL_DEPLOY_HOOK secret is not
+    # configured for this repo; the upstream pipeline expects a
+    # Nous-controlled Vercel project. Re-enable by adding a
+    # VERCEL_DEPLOY_HOOK secret for a Nadicode Vercel project and removing
+    # the leading `false &&`.
+    if: false && github.event_name == 'release'
     runs-on: ubuntu-latest
     steps:
       - name: Trigger Vercel Deploy
         run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
 
   deploy-docs:
-    if: github.repository == 'NousResearch/hermes-agent'
+    # Nadicode fork: gated off — upstream domain hermes-agent.nousresearch.com
+    # is owned by NousResearch, not nadicode. GitHub Pages is not configured
+    # for this repo and the build references the upstream domain via the
+    # llms.txt comments. Re-enable by configuring GitHub Pages, updating
+    # the staging step, replacing the upstream domain references with a
+    # Nadicode-controlled one, and removing the leading `false &&`.
+    if: false && github.repository == 'NousResearch/hermes-agent'
     runs-on: ubuntu-latest
     environment:
       name: github-pages
Index: argo/.github/workflows/skills-index.yml
===================================================================
--- argo.orig/.github/workflows/skills-index.yml
+++ argo/.github/workflows/skills-index.yml
@@ -47,7 +47,13 @@ jobs:
   # everything in one pipeline); we just kick it on a schedule.
   trigger-deploy:
     needs: build-index
-    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
+    # Nadicode fork: gated off — upstream domain hermes-agent.nousresearch.com
+    # is owned by NousResearch, not nadicode. This job re-triggers
+    # deploy-site.yml whose deploy-docs job publishes a CNAME for the
+    # upstream domain. Re-enable by configuring GitHub Pages for this fork,
+    # restoring a Nadicode-controlled domain, and removing the leading
+    # `false &&`.
+    if: false && (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch')
     runs-on: ubuntu-latest
     steps:
       - name: Trigger Deploy Site workflow
