# Minimal rename config for the positive-case fixture.
# Same shape as the real nadia-rename.yaml; deliberately omits the
# leakage_positive/ glob so the planted hermes hit IS reported.

mappings:
  - {from: "hermes", to: "nadia"}

exceptions: []

skip_contexts: []
