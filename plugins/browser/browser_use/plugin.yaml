name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed Nadia tool gateway. Also powers the 'Nadia Subscription' UX flow that bills usage to a Nadia subscription."
author: NadicodeAI
kind: backend
provides_browser_providers:
  - browser-use
